#pragma once
// Compile-time identity + capabilities for the M5Stack CoreS3 (the incumbent board).
#define PUCK_BOARD_ID    "m5cores3"
#define PUCK_OTA_SUFFIX  ""        // incumbent: keeps the existing unsuffixed OTA paths (no fleet transition)
#define PUCK_HAS_PSRAM   1
#define PUCK_ROUND       0         // rectangular 320x240
